name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  macos-app:
    # goreleaser creates the release; this attaches the app to it.
    needs: goreleaser
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      # 16 is the floor to read the project's file system synchronized group. 26 is
      # the floor for Liquid Glass: the app adopts it by linking against the macOS 26
      # SDK, so an older toolchain silently ships the pre-Tahoe look instead.
      - name: Check the Xcode version
        run: |
          xcodebuild -version
          major=$(xcodebuild -version | head -1 | sed -E 's/Xcode ([0-9]+).*/\1/')
          if [ "$major" -lt 26 ]; then
            echo "::error::Xcode $major cannot build with Liquid Glass; it needs 26 or newer"
            exit 1
          fi
      # Matching goreleaser, which strips the leading v, so the app and the CLI
      # report the same version.
      - name: Build the app
        run: VERSION="${GITHUB_REF_NAME#v}" ./scripts/build-app.sh
      - name: Package the app
        run: ditto -c -k --keepParent build/RedashWire.app "RedashWire_${GITHUB_REF_NAME#v}_macos_universal.zip"
      - name: Attach the app to the release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "$GITHUB_REF_NAME" "RedashWire_${GITHUB_REF_NAME#v}_macos_universal.zip" --clobber
